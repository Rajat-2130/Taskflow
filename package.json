{
  "name": "taskflow",
  "version": "1.0.0",
  "description": "TaskFlow - Team Task Management App",
  "scripts": {
    "start": "node taskflow/backend/server.js",
    "build": "cd taskflow/frontend && npm install && npm run build && cd ../backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}